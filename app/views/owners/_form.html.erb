<%= simple_form_for(@owner) do |f| %>
  <% if @owner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@owner.errors.count, "error") %> prohibited this owner from being saved:</h2>

      <ul>
      <% @owner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :nome %>
  </div>
  <div class="field">
    <%= f.input :cpf %>
  </div>
  <div class="field">
    <%= f.input :data_nascimento, start_year: Date.today.year - 90, end_year: Date.today.year %>
  </div>
  <div class="field">
    <%= f.input :contato %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
